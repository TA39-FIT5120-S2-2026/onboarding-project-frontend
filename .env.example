VITE_API_BASE_URL=http://localhost:3000

# Use bundled fixtures instead of calling the backend. Delete this flag
# before the Week 3 deployment - fixtures are for development only.
VITE_USE_FIXTURES=true
